# Copie este arquivo para backend/.env e preencha os valores.
# O .env real NUNCA e commitado (ver .gitignore e CLAUDE.md).

SECRET_KEY=troque-por-um-valor-aleatorio
DEBUG=True

# Execução local (backend fora do Docker):
DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/g7
# No Docker Compose, use o nome do serviço do PostgreSQL (por exemplo, db):
# DATABASE_URL=postgresql+psycopg2://usuario:senha@db:5432/g7
